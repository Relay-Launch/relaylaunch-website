---
import Layout from '../layouts/Layout.astro';
import { Button } from '@/components/starwind/button';
import ServiceIcons from '@/components/ServiceIcons.astro';

const serviceSchemas = [
  {
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: 'Complete Analysis: Business Infrastructure Diagnostic',
    provider: { '@type': 'Organization', name: 'RelayLaunch LLC', url: 'https://www.relaylaunch.com' },
    description: 'An 8-area diagnostic of your entire business infrastructure, covering operations, tech stack, AI readiness, digital footprint, and more, delivered as a 20–30 page professional report with a prioritized roadmap.',
    url: 'https://www.relaylaunch.com/complete-analysis/',
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'USD',
      lowPrice: '1500',
      highPrice: '3000',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: 'Launch: Digital Infrastructure Build',
    provider: { '@type': 'Organization', name: 'RelayLaunch LLC', url: 'https://www.relaylaunch.com' },
    description: 'One-time project build of unified digital infrastructure: custom website, automation workflows, email sequences, social scheduling, and a single connected dashboard replacing disconnected tools.',
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'USD',
      lowPrice: '2500',
      highPrice: '5000',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: 'Run: Monthly Digital Operations Retainer',
    provider: { '@type': 'Organization', name: 'RelayLaunch LLC', url: 'https://www.relaylaunch.com' },
    description: 'Monthly retainer for ongoing digital operations support: social content, SEO and blog maintenance, automation monitoring, and monthly strategy calls. 3-month minimum.',
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'USD',
      lowPrice: '500',
      highPrice: '1000',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: 'Scale: Premium Growth Retainer',
    provider: { '@type': 'Organization', name: 'RelayLaunch LLC', url: 'https://www.relaylaunch.com' },
    description: 'Premium monthly retainer for businesses scaling their digital infrastructure: expanded deliverables, priority support, advanced automation builds, and strategic advisory. 6-month minimum.',
    offers: {
      '@type': 'AggregateOffer',
      priceCurrency: 'USD',
      lowPrice: '1000',
      highPrice: '2500',
    },
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: [
    { '@type': 'Question', name: 'Do I have to commit to a retainer right away?', acceptedAnswer: { '@type': 'Answer', text: 'No. The Complete Analysis is a standalone engagement with no strings attached. You get a 20–30 page report, keep it, and decide what to do next.' } },
    { '@type': 'Question', name: 'Who owns what you build?', acceptedAnswer: { '@type': 'Answer', text: 'You own 100% of everything we build: website, code, automations, content, accounts, and domains. No vendor lock-in.' } },
    { '@type': 'Question', name: 'How long does a Complete Analysis take?', acceptedAnswer: { '@type': 'Answer', text: 'Two business days from intake completion. You receive the deliverable within 5 business days of kickoff.' } },
    { '@type': 'Question', name: "What's the difference between Launch and Run?", acceptedAnswer: { '@type': 'Answer', text: 'Launch is a one-time build. We design and deploy your unified infrastructure. Run is ongoing operations. We stay on as your digital ops team.' } },
    { '@type': 'Question', name: 'Can I switch tiers once I start?', acceptedAnswer: { '@type': 'Answer', text: 'Yes. You can upgrade from Run to Scale at any renewal point. Downgrading follows the same cadence.' } },
    { '@type': 'Question', name: 'Do I need to be local to Boston?', acceptedAnswer: { '@type': 'Answer', text: 'No. We work with small businesses across the U.S. remotely. Boston and the Northeast get priority scheduling.' } },
    { '@type': 'Question', name: 'What if I\'m already mid-build with another provider?', acceptedAnswer: { '@type': 'Answer', text: 'We can audit what exists and take it from there. The Complete Analysis covers your current stack regardless of who built it.' } },
    { '@type': 'Question', name: 'How is this different from hiring a freelancer?', acceptedAnswer: { '@type': 'Answer', text: 'Freelancers deliver a project and move on. We deliver infrastructure designed to run your business, then stay accountable to its performance.' } },
  ],
};
---

<Layout
  title="Services | RelayLaunch | Digital Infrastructure for Small Business"
  description="RelayLaunch service packages: Complete Analysis diagnostic ($1,500–$3,000), Launch build ($2,500–$5,000), Run retainer ($500–$1,000/mo), Scale retainer ($1,000–$2,500/mo)."
  canonicalUrl="https://www.relaylaunch.com/services/"
  jsonLd={[...serviceSchemas, faqSchema]}
  breadcrumbs={[
    { name: 'Home', url: 'https://www.relaylaunch.com/' },
    { name: 'Services', url: 'https://www.relaylaunch.com/services/' },
  ]}
>

  <!-- ═══════════════════════════════════════════════ HERO -->
  <section class="section svc-hero">
    <div class="svc-hero-grid" aria-hidden="true"></div>
    <div class="container">
      <div class="svc-hero-content">
        <span class="svc-hero-eyebrow">Service Packages</span>
        <h1 class="svc-hero-headline">
          Clear scope. Fixed price.<br />
          <span class="svc-accent">No surprises.</span>
        </h1>
        <p class="svc-hero-sub">
          Every package has defined deliverables and transparent pricing. We
          have a conversation before anything starts, and you'll know exactly
          what you're getting. Pick where you want to begin.
        </p>
        <div class="svc-hero-tiers">
          <a href="#complete-analysis-highlight" class="svc-tier-chip svc-tier-active">Complete Analysis</a>
          <a href="#launch" class="svc-tier-chip">Launch</a>
          <a href="#run" class="svc-tier-chip">Run</a>
          <a href="#scale" class="svc-tier-chip">Scale</a>
        </div>
        <div class="svc-hero-actions">
          <Button variant="primary" href="/contact" size="lg">Start a Conversation →</Button>
          <Button variant="outline" href="/complete-analysis" size="lg">Start with a Complete Analysis</Button>
        </div>
        <p class="svc-hero-note">
          No commitment required. Discovery call is free.
        </p>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════ RECOMMENDED FIRST STEP -->
  <section class="section" id="complete-analysis-highlight">
    <div class="container">
      <div class="ca-highlight card addon-card-featured">
        <div class="ca-highlight-inner">
          <div class="ca-highlight-text">
            <div class="addon-start-badge">Recommended First Step</div>
            <h2 style="font-size: clamp(24px, 3.5vw, 36px); font-weight: 800; margin-bottom: 8px; display: flex; align-items: center; gap: 10px;"><ServiceIcons icon="complete-analysis" size={32} /> Complete Analysis</h2>
            <div class="ca-highlight-price price-primary">$1,500–$3,000</div>
            <p style="color: var(--color-muted); font-size: 15px; line-height: 1.7; margin-bottom: 20px;">
              Before you spend a dollar on building, know exactly what needs building. A two-day
              deep dive across eight areas of your business, delivered as a 20–30 page report
              with a prioritized roadmap. You keep it whether you hire us or not.
            </p>
            <ul class="checklist">
              <li>8-area business infrastructure audit</li>
              <li>Tool stack & ops review</li>
              <li>AI readiness assessment</li>
              <li>Prioritized roadmap with cost projections</li>
            </ul>
            <div style="margin-top: 24px; display: flex; gap: 12px; flex-wrap: wrap;">
              <Button variant="primary" href="/complete-analysis" size="lg">Learn More →</Button>
              <Button variant="outline" href="/contact" size="lg">Book a Discovery Call</Button>
            </div>
          </div>
          <div class="ca-highlight-sidebar">
            <div class="ca-highlight-stat">
              <div class="stat-number" style="color: var(--color-primary); font-size: 28px;">2 days</div>
              <div class="stat-label">from intake to deliverable</div>
            </div>
            <div class="ca-highlight-stat">
              <div class="stat-number" style="color: var(--color-primary); font-size: 28px;">20–30 pg</div>
              <div class="stat-label">professional roadmap report</div>
            </div>
            <div class="ca-highlight-stat">
              <div class="stat-number" style="color: var(--color-primary); font-size: 28px;">$0</div>
              <div class="stat-label">commitment beyond the analysis</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════ SERVICE TIERS -->
  <section class="section" id="tiers">
    <div class="container">
      <span class="section-eyebrow">Service Tiers</span>
      <h2 class="section-title" style="margin-bottom: 16px;">Three tiers. Pick the one that fits.</h2>
      <p class="section-sub" style="max-width: var(--container-narrow); margin-bottom: 48px;">
        Need a one-time build? A team that stays on and runs things monthly? Or a full growth engine
        with AI agents and strategic advisory? There's a tier for wherever you are right now.
      </p>

      <div class="tiers-grid reveal-stagger">
        <div class="card tier-card">
          <span class="section-eyebrow" style="margin-bottom: 12px;">Tier 1</span>
          <h3 id="launch" class="tier-name"><ServiceIcons icon="launch" size={28} /> Launch</h3>
          <div class="tier-price price-primary">$2,500–$5,000 <span class="price-label">one-time</span></div>
          <p class="tier-desc">Your digital foundation, built right the first time. Professional website, core automations, connected tools — all delivered in one engagement. You walk away with a system that works.</p>
          <h4 class="tier-includes-title">Includes</h4>
          <ul class="checklist">
            <li>Custom website (5–10 pages)</li>
            <li>3–5 automation workflows</li>
            <li>Email sequence setup</li>
            <li>Tool consolidation &amp; integration</li>
            <li>Unified dashboard</li>
            <li>30-day launch support</li>
          </ul>
          <Button variant="outline" href="/contact" class="mt-4 w-full justify-center">Get Started →</Button>
        </div>

        <div class="card tier-card tier-featured">
          <div class="badge badge-solid-primary" style="margin-bottom: 12px;">Most Popular</div>
          <span class="section-eyebrow" style="margin-bottom: 12px;">Tier 2</span>
          <h3 id="run" class="tier-name"><ServiceIcons icon="run" size={28} /> Run</h3>
          <div class="tier-price price-primary">$500–$1,000<span class="price-label">/month</span></div>
          <p class="tier-note">3-month minimum</p>
          <p class="tier-desc">Your ops team on retainer. We keep systems optimized, content flowing, and automations evolving month over month. You focus on clients — we handle the digital infrastructure.</p>
          <h4 class="tier-includes-title">Includes</h4>
          <ul class="checklist">
            <li>Everything in Launch</li>
            <li>Monthly content (blog + social)</li>
            <li>Automation monitoring &amp; new workflows</li>
            <li>SEO optimization &amp; reporting</li>
            <li>Monthly strategy call</li>
            <li>Priority Slack support</li>
          </ul>
          <Button variant="primary" href="/contact" class="mt-4 w-full justify-center">Start Running →</Button>
        </div>

        <div class="card tier-card">
          <span class="section-eyebrow" style="margin-bottom: 12px;">Tier 3</span>
          <h3 id="scale" class="tier-name"><ServiceIcons icon="scale" size={28} /> Scale</h3>
          <div class="tier-price price-primary">$1,000–$2,500<span class="price-label">/month</span></div>
          <p class="tier-note">6-month minimum</p>
          <p class="tier-desc">AI agents handling content, outreach, and strategy on top of everything in Run. Advanced automations and a dedicated advisor in your corner. For businesses ready to grow, not just maintain.</p>
          <h4 class="tier-includes-title">Includes</h4>
          <ul class="checklist">
            <li>Everything in Run</li>
            <li>AI agent setup &amp; management</li>
            <li>Advanced workflow automation</li>
            <li>Multi-location or multi-brand support</li>
            <li>Quarterly business review</li>
            <li>Fractional AI Ops advisory</li>
          </ul>
          <Button variant="outline" href="/contact" class="mt-4 w-full justify-center">Let's Scale →</Button>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════ ADDITIONAL ENGAGEMENTS -->
  <section class="section" id="additional">
    <div class="container">
      <span class="section-eyebrow">Additional Engagements</span>
      <h2 class="section-title" style="margin-bottom: 16px;">Standalone services you can add anytime.</h2>
      <p class="section-sub" style="max-width: var(--container-narrow); margin-bottom: 48px;">
        These engagements can be added to any tier or purchased independently.
      </p>

      <div class="addons-grid">
        <div class="card addon-card">
          <h3>AI Agent Setup</h3>
          <div class="addon-price price-secondary">$800–$2,000</div>
          <p>Design and deploy AI-powered agents for customer support, lead qualification, content generation, or internal knowledge retrieval, all integrated into your existing tools.</p>
        </div>

        <div class="card addon-card">
          <h3>Workflow Automation Build</h3>
          <div class="addon-price price-secondary">$500–$1,500 per workflow</div>
          <p>Custom n8n workflow design, build, and deployment. Connect your CRM, email, scheduling, invoicing, and any tool with an API into automated sequences.</p>
        </div>

        <div class="card addon-card">
          <h3>Systems Integration</h3>
          <div class="addon-price price-secondary">$100–$175/hour</div>
          <p>For businesses that have tools but need them connected. API integrations, data mapping, platform consolidation, and workflow design, billed hourly.</p>
        </div>

        <div class="card addon-card">
          <h3>Fractional AI Ops Officer</h3>
          <div class="addon-price price-secondary">$1,500–$3,000/month</div>
          <p>A dedicated AI operations advisor working inside your business. Finds the automations worth building, manages the AI tools, and spots the time and money savings you'd miss from inside the day-to-day.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════ FAQ -->
  <section class="section" id="faq">
    <div class="container">
      <span class="section-eyebrow">Common Questions</span>
      <h2 class="section-title" style="margin-bottom: 12px;">Questions we hear on almost every discovery call.</h2>
      <p class="section-sub" style="max-width: var(--container-narrow); margin-bottom: 48px;">
        Honest answers. No fine print. No surprises.
      </p>

      <div class="faq-list reveal-stagger">
        <details class="faq-item">
          <summary class="faq-question">Do I have to commit to a retainer right away?</summary>
          <div class="faq-answer">
            <p>No. The Complete Analysis is a standalone engagement with no strings attached. You get a 20–30 page report, keep it, and decide what to do next. Hire us, DIY it, or do nothing for now. Most clients choose to continue, but that's entirely your call.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">Who owns what you build?</summary>
          <div class="faq-answer">
            <p>You own 100% of everything we build: website, code, automations, content, accounts, and domains. No vendor lock-in. No dependencies. If you walk away tomorrow, your systems keep running.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">How long does a Complete Analysis take?</summary>
          <div class="faq-answer">
            <p>Two business days from intake completion. Day 1 is the deep audit across 8 areas of your business. Day 2 is report writing and review. You receive the deliverable within 5 business days of kickoff.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">What's the difference between Launch and Run?</summary>
          <div class="faq-answer">
            <p><strong>Launch</strong> is a one-time build. We design and deploy your unified infrastructure, hand it off, and you run it. <strong>Run</strong> is ongoing operations. We stay on as your digital ops team, keeping systems optimized and content flowing. Most businesses start with Launch, then add Run once the foundation is solid.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">Can I switch tiers once I start?</summary>
          <div class="faq-answer">
            <p>Yes. You can upgrade from Run to Scale at any renewal point (monthly). Downgrading follows the same cadence. We'll never lock you into a tier that no longer fits where your business is.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">Do I need to be local to Boston?</summary>
          <div class="faq-answer">
            <p>No. We work with small businesses across the U.S. remotely. Discovery calls, presentations, and strategy sessions are all via video. Boston and the Northeast get priority scheduling, but geography is never a barrier.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">What if I'm already mid-build with another provider?</summary>
          <div class="faq-answer">
            <p>We can audit what exists and take it from there. The Complete Analysis covers your current stack regardless of who built it. We're not here to start from zero if there's solid work worth preserving.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary class="faq-question">How is this different from hiring a freelancer?</summary>
          <div class="faq-answer">
            <p>Freelancers deliver a project and move on. We deliver infrastructure designed to run your business, then stay accountable to its performance. We combine strategy, build, and operations in a single engagement, not three separate contracts with three separate people.</p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════ CTA -->
  <section class="section">
    <div class="container" style="text-align: center;">
      <h2 class="section-title">Not sure where to start? That's normal.</h2>
      <p class="section-sub" style="margin: 16px auto 32px; max-width: var(--text-width-sm);">
        Most clients start with a conversation. Tell us where you are and what's not
        working. We'll be honest about whether we can help — and which package makes
        sense for your stage.
      </p>
      <Button variant="primary" href="/contact">Let's Talk →</Button>
      <p class="cta-no-commit">No commitment required. Discovery call is free.</p>
    </div>
  </section>

</Layout>

<script>
  // FAQ smooth accordion animation (P0-5)
  document.querySelectorAll('.faq-item').forEach((details) => {
    const answer = details.querySelector('.faq-answer') as HTMLElement;
    if (!answer) return;

    details.addEventListener('click', (e) => {
      const summary = (e.target as HTMLElement).closest('summary');
      if (!summary) return;

      e.preventDefault();

      if (details.hasAttribute('open')) {
        // Closing: animate then remove open
        answer.style.maxHeight = answer.scrollHeight + 'px';
        requestAnimationFrame(() => {
          answer.style.maxHeight = '0';
          answer.style.paddingBottom = '0';
        });
        answer.addEventListener('transitionend', () => {
          details.removeAttribute('open');
          answer.style.maxHeight = '';
          answer.style.paddingBottom = '';
        }, { once: true });
      } else {
        // Opening: set open then animate
        details.setAttribute('open', '');
        const height = answer.scrollHeight;
        answer.style.maxHeight = '0';
        answer.style.paddingBottom = '0';
        requestAnimationFrame(() => {
          answer.style.maxHeight = height + 'px';
          answer.style.paddingBottom = '20px';
        });
        answer.addEventListener('transitionend', () => {
          answer.style.maxHeight = '';
        }, { once: true });
      }
    });
  });
</script>

<style>
  /* ── Services Hero (distinct from homepage) ── */
  .svc-hero {
    background: var(--color-navy);
    color: var(--color-white);
    position: relative;
    overflow: hidden;
    padding-top: 100px;
    padding-bottom: 80px;
    border-top: none;
  }

  .svc-hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(color-mix(in srgb, var(--primary) 5%, transparent) 1px, transparent 1px),
      linear-gradient(90deg, color-mix(in srgb, var(--primary) 5%, transparent) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse 60% 50% at 80% 50%, black 20%, transparent 100%);
    -webkit-mask-image: radial-gradient(ellipse 60% 50% at 80% 50%, black 20%, transparent 100%);
    pointer-events: none;
  }

  .svc-hero-content {
    position: relative;
    z-index: 1;
    max-width: 680px;
  }

  .svc-hero-eyebrow {
    display: inline-block;
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.6px;
    color: var(--color-primary);
    margin-bottom: 20px;
  }

  .svc-hero-headline {
    font-size: clamp(32px, 5vw, 52px);
    font-weight: 800;
    color: var(--color-white);
    line-height: 1.1;
    margin-bottom: 20px;
  }

  .svc-accent {
    color: var(--color-primary);
    position: relative;
    display: inline-block;
  }

  .svc-accent::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--color-primary), color-mix(in srgb, var(--primary) 40%, transparent));
    border-radius: 2px;
  }

  .svc-hero-sub {
    font-size: 17px;
    color: color-mix(in srgb, var(--color-white) 72%, transparent);
    line-height: 1.65;
    margin-bottom: 24px;
  }

  .svc-hero-tiers {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 28px;
  }

  .svc-tier-chip {
    display: inline-block;
    padding: 6px 14px;
    font-size: 12px;
    font-weight: 600;
    color: color-mix(in srgb, var(--color-white) 65%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-white) 15%, transparent);
    border-radius: 20px;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .svc-tier-chip:hover {
    color: var(--color-white);
    border-color: var(--color-primary);
    background: color-mix(in srgb, var(--color-primary) 12%, transparent);
    text-decoration: none;
  }

  .svc-tier-active {
    color: var(--color-white);
    border-color: var(--color-primary);
    background: color-mix(in srgb, var(--color-primary) 18%, transparent);
  }

  .svc-hero-actions {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .svc-hero-actions :global(a) {
    text-decoration: none;
  }

  .svc-hero-actions :global(.btn-outline) {
    border-color: color-mix(in srgb, var(--color-white) 30%, transparent);
    color: var(--color-white);
  }

  .svc-hero-actions :global(.btn-outline:hover) {
    border-color: var(--color-white);
    background: color-mix(in srgb, var(--color-white) 8%, transparent);
  }

  .svc-hero-note {
    margin-top: 14px;
    font-size: 13px;
    color: color-mix(in srgb, var(--color-white) 50%, transparent);
  }

  @media (max-width: 768px) {
    .svc-hero { padding-top: 72px; padding-bottom: 56px; }
    .svc-hero-headline { font-size: clamp(26px, 6vw, 40px); }
    .svc-hero-actions { flex-direction: column; width: 100%; }
    .svc-hero-actions :global(a) { width: 100%; justify-content: center; }
    .svc-hero-grid { display: none; }
  }

  /* ── Unified Price Display ── */
  .price-primary { font-size: 22px; font-weight: 800; }
  .price-secondary { font-size: 18px; font-weight: 700; }
  .price-label { font-size: 14px; font-weight: 500; }

  /* ── Package sections ── */
  .pkg-section { border-top: 1px solid var(--color-border); }
  .featured-pkg { background: color-mix(in srgb, var(--primary) 4%, var(--color-bg)); }

  .pkg-inner { display: flex; flex-direction: column; gap: 40px; }

  .pkg-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    align-items: start;
  }

  .pkg-number {
    display: block;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 2px;
    color: var(--color-muted);
    margin-bottom: 8px;
  }

  .pkg-title {
    font-size: clamp(32px, 4vw, 48px);
    font-weight: 800;
    margin-bottom: 6px;
  }

  .pkg-subtitle {
    font-size: 15px;
    color: var(--color-muted);
    margin-bottom: 12px;
  }

  .pkg-price-tag {
    color: var(--color-primary);
  }

  .pkg-price-tag span {
    color: var(--color-muted);
    margin-left: 4px;
  }

  .pkg-quote {
    font-size: 17px;
    color: var(--color-muted);
    font-style: italic;
    border-left: 3px solid var(--color-primary);
    padding-left: 20px;
    margin-top: 8px;
    line-height: 1.6;
  }

  .pkg-body {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 40px;
    align-items: start;
  }

  .pkg-desc {
    font-size: 15px;
    color: var(--color-muted);
    line-height: 1.7;
  }

  .deliverables-title {
    font-size: 13px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--color-muted);
    margin: 28px 0 16px;
  }

  .best-for { padding: 20px; }

  /* ── Service Tiers ── */
  .tiers-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    align-items: start;
  }

  .tier-card {
    padding: 32px 28px;
    display: flex;
    flex-direction: column;
  }

  .tier-featured {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 1px var(--color-primary), var(--shadow-md);
    background: linear-gradient(135deg, var(--color-surface) 0%, color-mix(in srgb, var(--primary) 3%, var(--color-surface)) 100%);
  }

  .tier-name {
    font-size: 28px;
    font-weight: 800;
    margin-bottom: 8px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .tier-price {
    color: var(--color-primary);
    margin-bottom: 4px;
  }

  .tier-price .price-label {
    color: var(--color-muted);
    margin-left: 2px;
  }

  .tier-note {
    font-size: 13px;
    color: var(--color-muted);
    margin-bottom: 4px;
  }

  .tier-desc {
    font-size: 14px;
    line-height: 1.65;
    color: var(--color-muted);
    margin-bottom: 20px;
  }

  .tier-includes-title {
    font-size: 12px;
    font-weight: 700;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--color-muted);
    margin-bottom: 12px;
  }

  /* ── Additional Engagements ── */
  .addons-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .addon-card {
    padding: 28px;
    display: flex;
    flex-direction: column;
  }

  .addon-card h3 { font-size: 18px; font-weight: 800; margin-bottom: 8px; }

  .addon-price {
    color: var(--color-primary);
    margin-bottom: 12px;
  }

  .addon-card p { font-size: 14px; line-height: 1.65; color: var(--color-muted); }

  .addon-card-featured {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 1px var(--color-primary), var(--shadow-sm);
    position: relative;
    background: linear-gradient(135deg, var(--color-surface) 0%, color-mix(in srgb, var(--primary) 3%, var(--color-surface)) 100%);
  }

  .addon-start-badge {
    display: inline-block;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.8px;
    text-transform: uppercase;
    color: var(--color-primary);
    background: color-mix(in srgb, var(--primary) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--primary) 25%, transparent);
    border-radius: var(--radius-xs);
    padding: 3px 10px;
    margin-bottom: 12px;
  }

  /* ── Complete Analysis Highlight (P0-1) ── */
  .ca-highlight {
    padding: var(--card-padding);
  }

  .ca-highlight-inner {
    display: grid;
    grid-template-columns: 1fr 280px;
    gap: 40px;
    align-items: start;
  }

  .ca-highlight-price {
    color: var(--color-primary);
    margin-bottom: 12px;
  }

  .ca-highlight-sidebar {
    display: flex;
    flex-direction: column;
    gap: 24px;
    padding: 24px;
    background: var(--color-surface-2);
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
  }

  .ca-highlight-stat {
    text-align: center;
  }

  @media (max-width: 768px) {
    .ca-highlight-inner { grid-template-columns: 1fr; }
  }

  /* ── FAQ ── */
  .faq-list {
    max-width: 760px;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .faq-item {
    border-bottom: 1px solid var(--color-border);
  }

  .faq-item:first-child {
    border-top: 1px solid var(--color-border);
  }

  .faq-question {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 0;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    list-style: none;
    user-select: none;
    color: var(--color-text);
    gap: 16px;
    transition: color 0.15s ease;
  }

  .faq-question:hover {
    color: var(--color-primary);
  }

  .faq-question::-webkit-details-marker { display: none; }

  .faq-question::after {
    content: '+';
    font-size: 20px;
    font-weight: 400;
    color: var(--color-primary);
    flex-shrink: 0;
    transition: transform 0.25s cubic-bezier(.22,1,.36,1);
  }

  .faq-item[open] .faq-question::after {
    content: '−';
  }

  .faq-answer {
    overflow: hidden;
    max-height: 0;
    padding: 0;
    transition: max-height 0.3s cubic-bezier(.22,1,.36,1),
                padding 0.3s cubic-bezier(.22,1,.36,1);
  }

  .faq-item[open] .faq-answer {
    max-height: 300px;
    padding: 0 0 20px;
  }

  .faq-answer p {
    font-size: 15px;
    line-height: 1.7;
    color: var(--color-muted);
    margin: 0;
  }

  .faq-answer strong {
    color: var(--color-text);
  }

  /* ── CTA micro-copy ── */
  .cta-no-commit {
    margin-top: 14px;
    font-size: 13px;
    color: var(--color-muted);
  }

  @media (max-width: 1024px) {
    .addons-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    .pkg-header, .pkg-body { grid-template-columns: 1fr; }
    .tiers-grid { grid-template-columns: 1fr; }
    .addons-grid { grid-template-columns: 1fr; }
  }
</style>
