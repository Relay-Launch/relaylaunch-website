# Lead magnet webhook (receives name + email JSON on form submission)
PUBLIC_LEAD_MAGNET_WEBHOOK_URL=https://your-webhook-url.example.com

# Client intake questionnaire webhook (receives form JSON on submission)
PUBLIC_INTAKE_WEBHOOK_URL=https://your-webhook-url.example.com

# Contact / Complete Analysis webhook (receives form JSON on submission)
PUBLIC_CONTACT_WEBHOOK_URL=https://your-webhook-url.example.com

# Control Center waitlist webhook (receives email + source JSON on signup)
PUBLIC_WAITLIST_WEBHOOK_URL=https://your-webhook-url.example.com

# Google Analytics 4 Measurement ID (only loaded in production builds)
PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Cal.com scheduling link (username/event-slug)
PUBLIC_CAL_LINK=victor-medina-vh0z5e/30min
